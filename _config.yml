defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
    values:
      layout: "nothing"
  -
    scope:
      path: "" # an empty string here means all files in the project
      type: "pages"
    values:
      layout: "default"
  -
    scope:
      path: ""
      type: "posts"
    values:
      layout: "post"


permalink: /:categories/:year/:month/:day/:title #:output_ext

title: "PureAsbestos.com"

collections:
  - fonts